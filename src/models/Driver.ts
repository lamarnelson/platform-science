/**
 * Driver.ts
 *
 * This file contains the Driver interface, which represents a driver entity.
 * It includes the name property to store the name of the driver.
 */

/**
 * Driver
 *
 * Interface representing a driver entity.
 */
export interface Driver {
  /**
   * The name of the driver.
   */
  name: string;
}
